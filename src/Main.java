import com.gestionBiblio.dao.DaoFactory;
import com.gestionBiblio.dao.EmprunteurDao;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public void main(String[] args) {
        // Press Alt+Entrée with your caret at the highlighted text to see how
        // IntelliJ IDEA suggests fixing it.
        System.out.printf("Hello and welcome!");
         EmprunteurDao emprunteurDao;

        public void init() throws ServletException {
            DaoFactory daoFactory = DaoFactory.getInstance();
            this.emprunteurDao = daoFactory.getEmprunteurDao();
        }

        // Appeler la méthode lister pour obtenir une liste d'emprunteurs



    }
}